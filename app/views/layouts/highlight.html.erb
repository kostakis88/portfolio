<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'highlights', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'shared/highlights_nav' %>
    <%= render 'shared/highlights_header' unless logged_in?(:site_admin) %>
    <%= yield %>
    <%= render 'shared/highlights_footer' %>
    <%= alerts %>
    <%= source_helper %> 
  </body>
</html>